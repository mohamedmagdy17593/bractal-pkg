---
name: Buttons
menu: Components
---

import {Playground} from 'docz'
import Button from './Button.jsx'
import styled from '@emotion/styled'

export default (function() {
  const Container = styled.div({
    '.react-live-preview > *': {margin: 5},
  })
  return props => <Container {...props} />
})()

### Basic Example

<Playground>
  <Button>Click This</Button>
</Playground>

### Sizing

<Playground>
  <Button xl>Click This</Button>
  <Button lg>Click This</Button>
  <Button>Click This</Button>
  <Button sm>Click This</Button>
  <Button xs>Click This</Button>
</Playground>

### Types

Two types are supported, `Primary` &amp; `Secondary`

<Playground>
  <Button>Click This</Button>
  <Button secondary>Click This</Button>
</Playground>

### Fill

You can go with the normal version, or inverted one

<Playground>
  <Button>Click This</Button>
  <Button inverted>Click This</Button>
  <Button inverted secondary>
    Click This
  </Button>
</Playground>

### Disabled State

You can go with the disabled version, or normal one

<Playground>
  <Button>Click This</Button>
  <Button disabled>Click This</Button>
  <Button inverted>Click This</Button>
  <Button disabled inverted>
    Click This
  </Button>
  <Button secondary>Click This</Button>
  <Button disabled secondary>
    Click This
  </Button>
  <Button inverted secondary>
    Click This
  </Button>
  <Button disabled inverted secondary>
    Click This
  </Button>
</Playground>

### Corners

You can go for a full circle corner

<Playground>
  <Button fullRound>Click This</Button>
  <Button fullRound inverted>
    Click This
  </Button>
</Playground>

### Loading Icon

<Playground>
  <Button xs tight loading inverted>
    Click This
  </Button>
  <Button xs loading inverted>
    Click This
  </Button>
  <Button sm loading inverted>
    Click This
  </Button>
  <Button sm loading inverted>
    Click This
  </Button>
  <Button md loading secondary>
    Click This
  </Button>
  <Button lg loading secondary inverted>
    Click This
  </Button>
  <Button xl loading secondary>
    Click This
  </Button>
  <Button xxl loading secondary inverted>
    Click This
  </Button>
</Playground>

### Arbitrary Icon

<Playground>
  <Button iconName="far fa-save">Click This</Button>
  <Button loading iconName="far fa-save">
    Click This
  </Button>
  <Button loading inverted iconName="far fa-save">
    Click This
  </Button>
  <Button loading secondary iconName="far fa-save">
    Click This
  </Button>
  <Button loading secondary inverted iconName="far fa-save">
    Click This
  </Button>
</Playground>

### Label Style

semiBold, bold & extraBold are supported

<Playground>
  <Button>Regular</Button>
  <Button semiBold>Semi Bold (600)</Button>
  <Button bold>Bold (700)</Button>
  <Button extraBold>Extra Bold (900)</Button>
</Playground>

### Using size prop

semiBold, bold & extraBold are supported

<Playground>
  <Button size="xl">Click This</Button>
  <Button size="lg">Click This</Button>
  <Button>Click This</Button>
  <Button size="sm">Click This</Button>
  <Button size="xs">Click This</Button>
</Playground>

### Responsive button size

This text size will be :

- xs -> On media size from the XSmall devices and below
- sm -> On media size from the XSmall devices and above
- md -> On media size from the Mobile devices and above
- lg -> On media size from the Tablet devicesand above
- xl -> On media size from the Desktop devices and above
- xxl -> On media size from the Large Desktop devices and above

<Playground>
  <Button important size={['xs', 'sm', 'md', 'lg', 'xl', 'xxl']}>
    Responsive Button
  </Button>
</Playground>

### Padding customizations

#### Normal, tight, custom

<Playground>
  <Button tight>Normal</Button>
  <Button>Normal</Button>
  <Button pxRatio={4}>Wide horizontal Padding</Button>
</Playground>

#### Full Width

<Playground>
  <Button block>Normal</Button>
</Playground>
